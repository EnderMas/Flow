我会讲述一些问题，接下来你要听我说有哪些问题，然后帮我把它们列成列表。关于 Flow 网页，现在的问题如下：

1. Library 界面
   (a) 新项目的命名方式：去掉输入框后面的对勾。
   (b) 子任务排序：不再通过点击上下箭头排序，改为通过直接拖动子任务来进行实时排序。
   (c) 子任务完成状态：给子任务添加一个完成按钮。在原本显示的数字的位置前面加上一个圆圈，勾选后该子任务会被划掉，表示已完成。

2. Pomodoro（番茄钟）部分
   (a) 适配高度：现在的倒计时没有适配屏幕高度。即使在 2K 屏上全屏显示，它也只会保持在页尾区域的一定高度，而不是位于页面最底部，这限制了时间显示的大小。需要改为：页眉（Header）在最上方，页脚在最下方，中间其余地方全部填充倒计时（不要让倒计时超出屏幕）。
   (b) 启动方式：现在不是点击背景任意位置都能启动，这个需要修改。
   (c) 倒计时显示：当切换到其他屏幕时，倒计时显示得太小，建议改得更大、更醒目一些。
   (d) 暂停 Bug：如果番茄钟暂停了，此时其他地方能看到 Header 背景是灰色的，但看不到当前的倒计时数值，这个 Bug 需要修复。

3. Header 部分
   (a) 日期与时间：现在的实时日期和时间太小。不要使用和 Library 或 Pomodoro 一样的字体，而是和 Settings 采用同一种设计语言。
   (b) 布局调整：让日期占一行，时间占一行，两行叠在一起。整体高度保持在原来 Settings 一行的位置（相当于上下各占半行）。

————————————————————————————————————————————————————————

我接下来会接着说一些关于这个网页的问题，帮我整理成列表。

以下是融合后的功能改进建议列表，分为 AI 功能增强、番茄钟优化及通用交互改进三个模块：

一、 AI 功能增强 (AI Integration)
1. 
外力 DDL 机制
   • 为现有 DDL 任务新增“外力 DDL”属性。
   • 核心逻辑： 通过绑定具有现实约束力的外部事件（如：提前预订机票、支付保证金等）产生强制执行力，倒逼任务在截止日期前完成。
2. 
目标识别与辅助拆解
   • 智能检测： AI 自动识别表述模糊（如“我要瘦”）或体量过大（如“完成举世闻名作品”）的目标。
   • 操作入口： 在子项目列表与开始按钮之间新增“AI 复查”按钮。
   • 引导流程： 点击后 AI 读取项目名（最高目标），提醒并辅助用户将其拆解为可操作的子项目。
3. 
每日智能排期与引导
   • 自定义起点： 支持设置每日开始时间（如 06:00）。
   • 启动引导： 每日首次打开应用时，自动切换到Home主页面。AI 根据项目权重（用户设置+AI自主评定）与 DDL 评定优先级，在Home主页面建议当天的“启动项”。
   • 动态跟踪： 任务完成后，AI 引导进入下一环节；每日结束时，引导进行 Wrap Up 复盘总结。
   • 前置条件： 若 Library 为空，则不触发提醒；右上角新增 Home 主页面。




二、 番茄钟（Pomodoro）深度优化
1. 
输入逻辑修复
   • 分钟容错： 输入个位数（如“1”）时自动补齐为两位数（“01”），而非判定为无效。
   • 秒数编辑： 修复目前秒数修改无法保存的 Bug。
2. 
交互逻辑与重置机制
   • 编辑限制： 仅在番茄钟处于“未开始（初始态）”时允许编辑时长。一旦进入计时或暂停状态，禁止选中或直接修改时间。
   • 快捷重置： 暂停状态下，支持“双击时间区域”触发重置。
   • 视觉反馈： 触发重置时，背景由灰色闪烁一下红色，随后回归初始黑色。



三、 界面交互与任务管理 (UI/UX)
1.
视觉表现力
• 时间显示： 优化日期与时间的 UI 设计，增强其醒目程度，确保信息层级清晰。
2.
操作体验优化
• 拖动区域限定： 子项目由“全区域可拖动”改为“仅数字区域可拖动”。此举旨在解决拖动冲突，方便用户直接点击文字进行编辑。
3.
任务组织管理
• 主任务管理： 为每个主任务新增“归档”与“删除”按钮。
• 侧边栏扩展： 在左侧 Vault Index 下方同步新增“归档区”与“删除区”，方便任务的分类回收与查阅。
4.
项目权重配置
• 权重设置： 添加各个项目的项目权重设置，用户可根据紧要度自行选择。

——————————————————————————————————————————————————————————————

语言设置与 UI 改进建议：

1. 语言汉化策略
   (a) 装饰性文字：如 Home 顶部的 "Good evening" 或每日一言等大字标题，保持英文或引用诗词均可，这类文字主要起美化作用，语种不限。
   (b) 功能性文字：必须全部汉化。
       - 例如 "Vault is empty. Navigate to Library to manifest your first project" 应改为中文提示，明确告知用户当前为空以及后续操作。
       - Library 下的三个标签页：Active、Archived、Trash 分别改为“活跃”、“归档”和“垃圾桶”。
       - 优先级部分：Priority 改为“优先级”，其对应的等级 Low、Medium、High 等应改为“低”、“中”、“高”。
       - 外部截止日期：EXTDDL (External DDL) 改为中文展示，方便理解。

2. 交互与视觉优化
   (a) 标签页选中态：Library 的三个标签页在点击切换时，缺乏明显的选中反馈（如颜色变白或加粗），需要增加更醒目的视觉提示。
   (b) 垃圾桶功能：目前 Trash 缺少“清空垃圾桶”的选项，建议增加该功能，否则该栏位缺乏实际意义。
   (c) 优先级设置：
       - 目前 Critical 和 Do or Die 已关联四星和五星，建议取消前面的空行，直接展示五个星星图标供用户点选。
       - 点选第一颗星即为 Low，点选第五颗星即为 Do or Die。
   (d) AI 复查目标：
       - 现有的长方框设计过于沉重，建议简化为一个按钮。
       - 点击按钮后开始转圈表示“AI 正在复查”；若出现问题则显示感叹号或叉号；复查完成后显示笑脸等简洁图标。
   (e) 字体统一：右上角的时间显示不要使用 Monospace 字体，请统一改为 Library 模块使用的 Sans 字体。

3. 功能细节调整
   (a) 外部 DDL：这一块主要由 AI 辅助设置，用户通常不会手动填写。目前的文本框形式可以保留，但需明确其 AI 驱动的属性。现有的例子可以取消。
   (b) 番茄钟重置：番茄钟部分的重置逻辑需要重做。底下的 "Double click to reset" 提示可以保留，但整体功能需优化。

————————————————————————————————————————————————————————————

把接下来说的话总结成列表：

1. 库页面优先级显示优化
   (a) 现状：设置优先级后，仅在项目标题下方显示一个小星号。
   (b) 改进：请用不同颜色直接区分项目重要度。
   (c) 设计方案（均需实现，并在设置中提供切换按钮）：
       - 大色块：直接将整个项目背景改为对应颜色。
       - 竖条装饰：在项目最前端增加对应颜色的竖条。
       - 名称变色：直接改变项目名称的文字颜色。
   (d) 颜色对应标准：
       - 决战：红色
       - 极高：橙色
       - 高：黄色
       - 中：黄绿色
       - 低：绿色

2. AI 目标评估与拆解
   (a) 按钮简化：删掉后面所有的字，只保留一个非常简单的按钮。
   (b) 位置调整：移动到外力 DDL后面。（原来的DDL剩余时间的展示位置）

3. DDL 剩余时间展示方式
   (a) 位置调整：移动到标题右侧，归档和删除按钮左侧的中间位置。
   (b) 表现形式：不要用“几天几小时”这种文字，要用更直观、能感受到时间流逝的方法。
   (c) 建议方案：
       - 倒计时。
       - 图形化进度：例如点阵图，起始为 100%，随时间流逝逐渐减少（99%、98% 等）。
（若有其他更好的方法请提出）

4. 主页缩进问题
   (a) 库中有内容时，主页显示的“生成今日计划”等内容下方，“点击上方按钮”前的缩进请删除。

5. 番茄钟（Pomodoro）功能优化
   (a) 时间显示：将“25:00”拆解为两个独立的文本框（分钟和秒钟），中间的冒号保留但设为不可选中，防止误删。
   (b) 重置反馈：目前暂停后双击空白处重置时会闪蓝色（因为优先判断了一次继续），请改为闪红色（代表重置，即快速双击会触发这个状态。平常点击时若没有快速双击，则会正常变回蓝色），然后再变回黑色。

6. 设置页面（Header）清理
   (a) 现状：左上角已显示“设置”，但点开页面后，在 AI 引擎选择上方、Header 下方还有一个大的“设置”字样。
   (b) 改进：删除多余的“设置”标题。

——————————————————————————————————————————————————————————

把接下来说的话总结成列表，有一些要修改的地方：

1. 库与排序功能
   (a) 库位置的项目现在不可拖动，需要改为可以拖动排序。
   (b) 在“新建项目”左侧添加一个排序图标。点击后展开选项：支持按自定义、剩余时间、要紧度或创建时间排序，并提供升序和降序选择。

2. DDL 倒计时优化
   (a) 倒计时目前不够明显。需添加“点阵图”可选项，通过点阵图的变化来直观展示时间的流逝。
   (b) 实时显示：在“库”页面的项目索引末尾，所有设置了截止日期的项目都应实时显示剩余时间。例如，12 号、15 号、17 号截止的项目，后面要对应显示具体的倒计时。

3. 收藏项目功能与 Header 布局
   (a) 在项目块添加“星标”功能。用户可以选中并收藏一个项目（仅限一个）。
   (b) 收藏项目的 DDL 会常驻显示在 Header 部分。
   (c) 调整 Header 部分的排布顺序：当前页名称 -> 收藏任务倒计时（用红色大字表示） -> 番茄钟倒计时 -> Tab 切换 -> 当前时间与日期。
   (d) 注意：原来番茄钟不在进行状态时会居中显示，现在需按上述顺序排布。

4. 字体与界面细节
   (a) 统一更换字体：不要用等宽字体，全部改为 Sans 字体并加粗。
   (b) 倒计时显示：如果是倒计时状态，直接显示数值。
   (c) 删除多余字符：像“T-”这种前缀直接删掉。
   (d) 文本框加宽：番茄钟页面的“分”和“秒”以及“休息部分”的文本框太小，导致数值为 0 或文字较宽时会被切掉，需要稍微加宽。

5. 提示音与 Bug 修复
   (a) 提示音优化：目前提示音不够强。请在设置中预设几种提示音供选择，并添加“导入音频文件”的功能，允许用户自定义。
   (b) 闪屏 Bug：双击空白处重置时没有红色闪屏，这个暂时维持原样。但目前闪屏时整个页面会往上缩，导致页脚移位，需要修复这个布局 Bug，确保页脚固定在底部。

6. Bridge 功能（断点日志/海明威停顿点）
• 功能定义：在暂停或结束工作时记录具体进度，确保下次快速衔接。
• 入口设置：
   • 番茄钟页面：页脚右下角设“Bridge”圆角矩形按钮（仅选中任务时显示），点击重置番茄钟并弹窗。
   • 任务库：详情页“开始专注”按钮右侧设同款圆角矩形按钮，点击弹出记录窗。
• 交互说明：
   • 记录内容：用户输入当前项目具体进度的“海明威停顿点”。
   • 回显与跳转：
      • 主页：生产计划下方显示停顿点长条，点击可跳转至任务库对应位置。
      • 任务库：在任务条目醒目位置显示停顿点，点击直达番茄钟并自动开始，同时提醒进度。

——————————————————————————————————————————————————————————————

1. 进度概况
目前基本功能已完成，暂时不进行 AI 测试。接下来的工作重点在于优化网页设计。

2. Bridge 与断点逻辑优化
   (a) 允许多个不同任务的断点同时在主页出现。当主页同时出现多个不同任务的断点时，利用 AI 来决定各断点显示的优先级。
   (b) 统一名称：将“断点记录”与“断点日志”统一。
   (c) 点击跳转逻辑：
      - 主页中的断点点击后应跳转至对应的任务。
      - 库（项目索引）中的断点点击后应进入番茄钟。

3. 番茄钟界面调整
   (a) 时间显示：Header部分的番茄钟由居左改为居中。
   (b) 点击判定：
   Header部分的番茄钟的点击判定改为“点击倒计时”时，才会触发暂停或状态切换。

   这样做是为了防止点击其他位置时，出现番茄钟误触导致暂停的情况。


4. 其他交互优化

   （a）截止日期（DDL）的填报交互目前不够自然，需要点击多处才能完成调整。建议进行如下优化：
         - 默认日期设置为当天：例如2026年 3 月 10 日创建项目，DDL 默认即为 “2026/03/10 --:--”，时间部分默认留空。
         - 增加滚轮快速调节：除了点击输入外，支持鼠标悬浮在年/月/日/时/分上时，通过滚动滚轮直接增减悬浮位置的对应数值。（如悬浮在“年”上时，滚轮向上拨则年份相应增加；为实现分别进行判断，可能需要把原来的文本框逻辑进行修改）
         - 占位符区分：如果用户没有操作填入时间，默认应显示为“--:--”，以区分于“00:00”零点。
         - 新增清除按键：在DDL一栏的日历图标左侧增加一个清除按钮。对于没有截止日期的项，可以直接一键清除现有 DDL，避免逐个手动删除。

   (b) 海明威停顿点的弹出窗口需设置为居中显示，并适当放大。
具体调整建议如下：
目前，“海明威停顿点”的弹出窗口是在左上角显示，而且尺寸比较小。将其设置为居中显示，覆盖面积大约占屏幕的 80% 即可。

——————————————————————————————

把接下来说到的问题总结成列表：

1. 截止日期与时间编辑优化
   (a) 截止日期位置目前的鼠标滚动编辑功能很好，但要求增加点击编辑功能。用户应该能直接点击“年、月、日、时、分”进行数值修改。
   (b) 分钟的滚动步长优化：目前通过滑轮滚动时只能以 5 分钟为单位调整，请改为 1 分钟一跳。

2. 番茄钟页面调整
   (a) 保持全背景点击功能：番茄钟页面背景应支持点击以直接开始或暂停时间，保持与之前版本一致的状态。（只有Header部分的番茄钟进行了点击事件才会启停时间的更改）
   (b) Header 部分文字大小：目前的 Header 只有点击时间才能继续或暂停，这很好。但番茄钟的数字文字大小变小了，请务必调大，要足够醒目。
   (c) 按钮命名统一：右下角的“Bridge 日志”不要了，统一改名为“BRIDGE_”。
(d) 颜色对比优化：目前番茄钟在运行状态下是蓝色背景，而倒计时文字是红色的，这种颜色搭配非常刺眼。因此我建议：当番茄钟启动后，倒计时的数字文字应当改为红色边框加白色填充的形式。

3. 海明威停顿点交互逻辑改版
   既然目前的停顿点无法居中且显示效果不佳，我们换一种方案，取消弹窗模式：
   (a) 点击 Bridge 按钮后，自动跳转到对应任务页面。
   (b) 在对应任务的 外力DDL 区域下方、执行步骤区域上方的中间位置，直接嵌入一个“海明威停顿点”输入区域。
   (c) 用户直接在此区域输入，无需弹出窗口。

4. 断点历史归档设计
   目前实现效果没问题，但弹窗非常难看。建议为其提供一个额外的独立页面，具体的页面设计请先帮我想一个方案。

5. DDL 倒计时展示优化
   点阵消耗倒数本应在项目的右侧出现，但目前颜色太深，与背景融为一体了。请修改颜色，建议将其颜色与任务的紧急程度挂钩（改成一致的颜色）。

大概就是这些



——————————————————————————————

还有一些其他要优化的地儿，我先挨个说一下：

1. 库页面的排序按钮优化
   目前鼠标悬浮在按钮上时会弹出窗口，但鼠标移动到弹窗上时，弹窗就会消失，导致无法点击。
   (a) 建议取消悬浮弹窗，改为点击后出现下拉菜单（Drop down）。
   (b) 将“正序/反序”与“排序依据”分开。点击排序按钮后只显示排序依据，在其左边额外增加一个正序反序切换按钮，点击后直接按相应顺序进行排序。

2. 名称统一
   库位置右下角的“海明威停顿点”目前显示为 bridge_+，但番茄钟那边显示的是 bridge_日志。请把这两个地方的名称统一为BRIDGE_。

3. 库页面布局调整
   项目右侧有点阵图，但目前点阵图和收藏按钮挤在一起，视觉上非常压抑。
   (a) 建议把“星标任务”按键提前到项目前面的区域，额外添加一个可点击的按钮来设置星标。
   (b) 任务页面的右侧布局，建议给标题留 1/3 的空间，剩下的 2/3 以及其他空余地方都分配给点阵图。

4. 另外还有一个我不知道是不是问题。现在主页上会出现那个大字，只有 start 下划线了。按理来说，之前不是会根据时间出现 Good Evening 之类的这些内容吗？我觉得原来那种设计更好，建议还原成按照时间来进行主页面标语分配的设计，那样会更好一些。

5. 第五个问题是：当我从库里面点击了海明威停顿点进行复原，并开启番茄钟之后，这个海明威停顿点应该被“删除”掉。

具体逻辑如下：
1. 显示逻辑：它既不应该出现在主页，也不应该出现在库的“海明威停顿点”部分。
2. 痕迹保留：不要完全删除它，因为有时候我需要保留这些痕迹。
3. UI 设计：
   (a) 在库右下角的 Bridge 按钮左边，设计一个单独的小圆形归档图标按钮。
   (b) 点进去之后，可以看到在具体的日期和时间，留下了哪一个海明威存档点。

————————————————————————————————

点阵图位置调整 （占据更大空间（见图，大概要占据红色圈圈起来的部分），改为倒计时来让点阵逐渐减少；具体减少方法为“（截止日期-创建时间）/点阵个数”来决定每个点阵需要多久消失）

右上角的时间和左侧的分割线之间缺少一点空位

除此之外，要修改的地方主要有：

1. Header 部分功能优化
   (a) 为 Header 部分的番茄钟倒计时在设置中添加 Slider 选项，用于调整其大小和位置。
   (b) 这样我可以进行实时调整。目前字体大小偏小，虽然也能接受，但最好还是能自主调节。

2. 收藏 DDL 的位置逻辑
   (a) 目前Header部分的星标任务的 DDL 的位置是固定的，导致在“历史归档”页面中，当标题为四个字时会分两行显示，这不符合我的预期。
   (b) 建议将 DDL 改为贴着标题显示。标题长短改变时，DDL 的位置也随之移动，确保标题保持在同一行。

3. 番茄钟页面视觉调整
   (a) 番茄钟页面的文字颜色改回原来的。不要红色描边
   (b) 我之前指的是 Header 部分的 星标任务DDL 倒计时。当番茄钟正在运行时，红色的 DDL 倒计时非常刺眼。
   (c) 修改方案：请给这个红色的文字加一个黑色阴影；或者在设置里添加文字调整功能（阴影、描边、颜色、填充、大小等），直接全权交由我来配置。

4. 主页断点日志部分
   (a) 目前会标出项目和优先级，这个设计很棒。
   (b) 优化建议：去掉前面的 "bridge_断点日志" 前缀，将项目名放在主要位置。
   (c) 结构调整：第一行文本样式为小标题，同时直接用文本填充颜色代表优先级（如红色项目标题对应红色优先级，绿色对应绿色），另外在标题后紧跟优先级星级（但是不要用⭐5这种，而应该是⭐⭐⭐⭐⭐）；第二行文本样式为大标题，显示断点日志的具体内容。

5. 库页面与归档优化
   (a) “开始专注”按钮设计的有点难看，请重新设计一下。
   (b) 历史归档页面的标题问题：大标题应该是项目名称，而不是 "bridge_archive"。
   (c) 在该页面增加搜索或筛选功能，方便按具体日期搜索归档内容。
(d) 历史归档页面的进入按钮：
   移动到“断点日志”那一栏的附近，不要在右下角作为单独的按钮出现

以上就是目前需要调整的全部内容。


 ——————————————————————————————————————————————

Header 部分的星标任务倒计时，应该添加一个倒计时的最大单位。目前系统的最大单位是“天”，哪怕是三年的倒计时也会显示成一千多天，然后再显示多少时、多少分、多少秒。

我们需要在选项里添加一个设置，允许用户调整倒计时的最大单位：
1. 可选单位应包括：秒、分、时、天、月、年。
2. 每一个单位都应该有对应的最大单位设置。
3. 举例来说：如果我设置最大单位为“秒”，那么 300 多天的倒计时就会显示为对应的总秒数，并以秒作为计时基准。

除此之外，Header 部分的星标任务名称现在不够显眼。之后需要调整一下视觉效果，让它稍微更显眼一点，确保一眼就能看出来是哪一个任务在倒计时。


1. 库页面的项目点阵图优化
   (a) 给点阵图腾出足够多的空间。
   (b) 点阵图的数目设置为三行，目前大约是 100 个点，需要改为 198 个点（因为 200 个点无法在三行中平均分配）。
   (c) 星标按键的位置保持不变，依然放在项目名称的最前面。
   (d) 完善倒计时的点灭逻辑。目前点阵图是倒计时状态，但计算方法需要修正：要根据项目的“创建时间”到“截止日期 (DDL)”的总时长进行计算。
   (e) 每一个项目的点灭速率应该是动态的。例如：
      - 如果创建时间距离 DDL 还有 198 分钟，则每一分钟灭一个点。
      - 如果创建时间距离 DDL 仅剩 198 秒，则每一秒钟灭一个点。
   (f) 关于 DDL 时间，如果 DDL 是默认的日期和时间，即日期填写完整但时间留空，那么此时不显示倒计时。必须在日期和时间都填写完整的情况下，系统才会显示倒计时（无论是数字倒计时还是点阵图倒计时）。

2. 海明威停顿点的视觉调整
   项目中的海明威停顿点，现在的“历史归档”放在右上角位置非常好，但视觉上看起来不像一个可点击的按钮。你需要稍微调整一下，让它具备明显的按钮质感。（可以仿照底下的“添加步骤”按钮）

3. “开始专注”按钮的设计语言统一
   底部的“开始专注”按钮目前与整体设计语言不符，需要进行如下调整：
   (a) 常态下：采用圆角矩形描线设计，只有白色外轮廓线，内部不填充，文字为白色。
   (b) 悬停态（Hover）：当鼠标放上去时，背景变为蓝色，文字保持白色。
   这样设计可以与番茄钟整体的视觉风格保持一致。

4. Header 部分星标任务的 DDL 样式拆分
   建议将这一块拆分成“平常的文字颜色”和“番茄钟运行时”两种样式。我不希望永久改变它的颜色，但在番茄钟运行期间，因为背景是蓝色，原来的红色会显得非常刺眼。所以请直接将其拆解为两种独立的样式，根据运行状态自动切换。

就这些。

——————————————————————————————————————————————————
把接下来说的话总结成列表，有以下几点需要修改：

1. DDL 倒计时与项目名称布局
   左上角的 DDL 倒计时做得很好，但建议将“星标项目名称”放在倒计时的上方一行。即将这一块拆成两行：上面一行是项目名，下面一行是倒计时，两者保持左对齐。同时，项目名的颜色应该随着优先级的颜色来改变。

2. 专注态颜色逻辑优化
   现在已经把专注态和常态颜色分开了，但专注态颜色的应用范围需要扩大。不仅要包括番茄钟页面的专注颜色，还应包括：当在其他页面时，如果番茄钟处于专注状态，Header 部分会显示蓝色背景，此时 Header 里的 DDL 倒计时颜色也应该自动切换为专注态颜色。

3. 点阵显示调整
   (a) 优先让点阵变成 5 行；如果不行，则尝试 4 行；若 4 行也不行，则保持 3 行。
   (b) 点数扩容：目前是 198 个点，请尝试扩大到 300 个。如果能塞下 5 行，则尝试将点数扩大到 600 个。或者可以在设置页面中添加一个 slider，让我来自行调整每行点数。

4. 时钟格式自定义
   右上角的时钟默认格式改为两行显示：
   (a) 第一行：月份 / 日期  周几
   (b) 第二行：小时:分钟:秒
   关于对齐方式，你可以考虑让日期保持（与第二行的时分秒）左对齐，周几保持（与第二行的时分秒）右对齐。

5. AI 引擎增强
   添加本地 AI 引擎支持（Ollama）：
   (a) 如果用户选择了 Ollama，除了输入 API 密钥，还需要一个选择模型的下拉菜单（Drop-down），请将其做进设置选项中。
   (b) 针对本地模型，添加一个“测试本地模型”按钮，点击后会向本地模型提交一次询问，可测试模型是否正在运行。

6. 设置页新增 Prompt 配置
   在“视觉与排版”和“引擎与系统”之外，再添加一个“Prompt”一栏（名称可以再美化一下）。这一栏用于给 AI 引擎提供自定义 Prompt，用户可以自行输入并保存。

7. 番茄钟音量控制
   番茄钟的提示音除了支持导入音频文件，请额外添加一个 Slider 滑块，用于调节提示音的音量大小。

8. 问候语字号调整
   主页的问候语字体稍微放得更大一些。如果拿不准具体数值，可以在设置里添加一个 Slider，让我可以自主调整字号大小。

——————————————————————————————————————————————————

总结我接下来说的话，列成列表：

1. 番茄钟时间编辑功能优化
   (a) 将番茄钟页面的专注时间（分、秒）改为可编辑状态，操作逻辑与库（Library）中 DDL 的编辑方式一致。
   (b) 在未启动状态下，既能点击选中进行编辑，也可以在鼠标悬浮时通过滚轮直接调整。
   (c) 休息时间的编辑逻辑也按此同步修改。

2. 番茄钟界面布局调整
   (a) 当专注时间结束并进入休息时间时，原本位于右下角的休息时间倒计时应放大并移至屏幕正中央（即原来专注时间的位置）。
   (b) 原本在中央的专注倒计时则移至右下角，显示格式为“专注：[起始时长]”。

3. 项目名称与星号颜色逻辑优化
   (a) 修复 Bug：目前启动番茄钟后切换标签页，页面背景颜色会随项目 DDL 的优先级颜色改变，导致视觉上非常晃眼。
   (b) 修改方案：无论项目优先级如何，页面背景应始终保持黑色，项目名称也一直保持白色。
   (c) 仅让项目名称前的“星号”随任务要紧程度自动变色。这样即便背景有颜色变化，也只有星号受影响，保证整体视觉舒适。
   (d) 将该星号添加纯黑阴影。

4. 视觉与排版设置个性化
   在设置中添加“各要紧程度颜色”的自定义功能。目前默认为红、橙、黄、绿等，请提供五个色块选择器，允许我手动配置每个要紧程度对应的具体色彩。

5. 修复页面偏移 Bug
   修复番茄钟专注时间结束时，整个页面会莫名其妙大幅度上移的问题。

6. 提醒机制修改
   (a) 专注时间/休息时间结束时，应同时进行闪屏和声音提醒。
   (b) 之前的逻辑是“一段时间不点击则持续闪屏但停止发声”，现改为：若不点击，则持续闪屏且持续发声，直到手动处理。
   (c) 其他发声规律保持不变。

————————————————————————————————————

1. 修复 Bug：目前启动番茄钟后切换标签页，页面背景颜色会随番茄钟页面背景颜色改变，导致视觉上非常晃眼。应该保证在“番茄钟”页面时，背景随番茄钟状态改变。 在其他页面时，背景保持黑色，只有Header部分的背景颜色随番茄钟状态改变。


2. 让 Header 部分的星标项目DDL 倒计时的颜色：1. 在番茄钟处于“专注状态”时，使用专注状态颜色2. 在番茄钟处于“休息状态”、“暂停状态”或者是“未启动状态”时，则默认使用常态颜色

3. 提醒机制修改

 专注结束时闪屏（闪屏为背景色与黑色互闪。若在番茄钟页面则为全屏闪烁，若在其他页面则为Header部分闪烁）（默认3s，在设置中添加自定义调整slider）+声音（默认提示音1次，在设置中添加自定义调整slider），若 5 分钟无未点击页面进行状态切换，则二次提醒，此时闪屏+声音持续直到点击（在设置中添加自定义调整slider）。

休息结束时亦如此，但是一二次提醒间隔缩短为90秒。 

另外，分离专注结束提示音和休息结束提示音（默认关联，可在设置中断开二者关联，并为二者分别调整）

4 修复页面偏移 Bug
   修复番茄钟专注时间结束时，无论在哪个页面都会出现的整个页面莫名其妙大幅度上移/页面下半部分消失的问题。

————————————————————


1. 番茄钟倒计时逻辑异常
   (a) 启动番茄钟后，切换到其他页面时 Header 部分的番茄钟会正常倒计时，但番茄钟主页面的倒计时却停止了。
   (b) 在番茄钟主页面时，Header 部分不应该显示倒计时，而主页面的主要倒计时应该正常运行。
   (c) 总结预期效果：其他页面显示 Header 倒计时；番茄钟页面隐藏 Header 倒计时，仅保持主页面大倒计时正常工作。

2. 重置时间交互优化
   (a) 目前重置时间会闪一下红屏，建议取消这个红色闪屏效果。
   (b) 双击重置后，页面颜色应直接回归黑色（代表未启动状态），而不是灰色（代表暂停状态）。

3. 优先级主题色彩矩阵失效
   (a) 现在的颜色默认全部变成了黑色，需要恢复原来的默认设置。
   (b) 原有颜色对应关系：极高为红色，高为橙色，中为黄色，中低为黄绿，低为绿色。

4. 设置页面的点阵图阵列规模 Bug
   (a) 调整行数和列数的 Slider 时，上方的数字不会实时变动。
   (b) 只有在调整其他 Slider 之后，该数字才会更新。

不知道这是什么 Bug，修一下就行。

——————————————————————————————————————

现在番茄钟专注时间结束时没有闪屏，请添加闪屏效果（闪屏为背景色与黑色互闪。若在番茄钟页面则为全屏闪烁，若在其他页面则为Header部分闪烁）

缩小番茄钟文本框高度，让文本框大小与文字大小相同。现在点击背景部分有时候会点击到文本框部分，会影响使用体验。另外，让倒计时在处于倒计时/暂停状态时不能点击文本框编辑。只允许倒计时在未启动状态时进行点击文本框/滚轮滚动编辑

“夺命题型机制配置”的设置slider在调整时无法实时更新，请修改这个bug

——————————————————————————————————————

1、新建步骤的时候自动选中方便直接填写，同时为步骤的文本框内容添加自动换行。另外，填写好一个步骤之后，按下 Enter 键可以直接添加新的步骤。
2、项目内容出现了因为屏幕太小导致需要滚动才能看到所有内容的情况，请将项目索引与项目内容之间的分割位置改为可手动调整，同时保证DDL时间、归档按钮等右对齐的控件保持随屏幕大小动态调整，不要出现左右滚动（上下滚动可以有）
3、开始添加音乐控件。在番茄钟页面内，我想要实现的效果有：

1. 自动播放与 API 选择
   (a) 打开番茄钟时会自动播放音乐。
   (b) 目前默认仅提供网易云音乐的 API，其他 API 暂时不提供，以减少维护成本。

2. 专注模式联动
   (a) 当点击“开始专注”时，页面会跳转至专注页面。
   (b) 音乐同步开始播放，默认播放“每日推荐”的内容。
   (c) 若“每日推荐”播放完毕，则自动切换至“我喜欢的音乐”，并以心动模式继续播放。

3. 音乐控件基础功能
   (a) 包含上一曲、暂停/播放、下一曲、音量调整及进度条调整以及播放方式（顺序播放、随机播放等），播放列表以及将音乐添加到“我喜欢的音乐”的按钮。
   (b) 显示音乐专辑封面及歌词。

目前的开发重点是先保证音乐 API 的连接与正常使用，之后的功能我们再一点一点添加。可以先考虑在番茄钟页面暂时添加一个入口，用来进入到音乐界面，然后在新的页面里面进行音乐控件的添加。暂时先不考虑把这个控件融入到番茄钟页面内。也就是说，暂时先不考虑复杂的设计，先保证功能齐全且可用。

————————————————————————————————

1. 优化项目具体内容右侧的滚动条显示：
   (a) 即使鼠标悬浮在页面上，也不要显示滚动条。
   (b) 仅在实际进行上下滚动操作时才出现，否则悬浮条停留在原处会影响美观。
   (c) 或者可以将悬浮条向右移动一些，腾出一点空间，避免目前与其它元素重叠的问题。

2. 优化步骤添加逻辑：
   (a) 使用 Enter 键添加步骤后，如果没有在其中输入任何内容（保持为空），应在失焦或完成操作后默认将其删除。

3. 恢复跳过功能：
   原来的“跳过当前休息阶段”或“跳过当前专注阶段”的按钮消失了，请把它重新添加回来。

4. 音乐控制台相关

目前音乐控制台的功能需要进一步完善，主要存在以下几个问题：

1. 切换切歌延迟问题
   在输入 API 和 Token 后可以正常使用，但进行“上一首”或“下一首”切换时，会有约 10 秒左右的延迟。我怀疑是由于缺少音乐缓存导致的。建议提供一定的缓存容量，以便更流畅地切换歌曲。如果不是缓存原因，也请设法解决切换时卡顿的问题。

2. 播放控制与逻辑优化
   (a) 缺少播放顺序调整按钮：目前缺少随机播放、顺序播放、单曲循环以及循环播放的切换功能。
   (b) “喜欢”按钮逻辑：现在的爱心按钮点击后可以添加歌曲到“我喜欢的音乐”，但再次点击无法取消（删除）。按理说应该实现“点一次添加，再点一次移除”的循环逻辑。
   (c) 播放模式支持：目前仅“每日推荐”模式可正常使用。缺少“读取我喜欢的乐曲”并应用“心动模式”的功能。

请针对以上几点进行修改和优化。
————————————————————————————————————————————————

对我接下来说的几点进行列表整理：

1. 音乐控制台优化
   (a) 切换延迟问题：在“每日推荐”和“我的喜欢”之间切换时有明显的延迟，请尽量降低这个延迟。
   (b) 新增心动模式：目前缺少网易云音乐 API 中的“心动模式”。该模式应在当前播放队列基础上自动推荐歌曲，确保播放列表不断流。
   (c) 心动模式逻辑：例如在 120 首歌的基础上先推荐 10 首随机插入队列，听完 10 首后再自动推荐 10 首。播放模式默认随机，播放会既包含收藏的音乐，也包含根据当前队列推荐的新音乐。（若与网易云音乐的“心动模式”推荐逻辑不同，优先以网易云音乐的推荐逻辑为准）

2. 番茄钟（Pomodoro）页面与音乐播放器融合
   (a) 自动播放：番茄钟每天第一次开始时，默认自动播放“每日推荐”。之后开始时不会影响播放状态（如果本来就在播放中的话，则不会造成任何影响；如果本身处于暂停状态，也不会继续播放，除非用户主动点击）（另外为自动播放添加一个设置，可以让用户自主选择：
1. 每天第一次开始时是否自动播放
2. 自动播放的内容是“每日推荐”还是“我喜欢的音乐”）
   (b) 布局调整：
       - 音乐控件：位于页面中下方，包含上一首、下一首、暂停/继续、红心（收藏）及播放方式按钮。
       - 播放队列：点击按钮后，在页面右侧显示当前播放队列，并配备进度条。
       - 歌词显示：页面左侧显示歌词，并添加一个歌词开关按钮。
   (c) 背景视觉：
       - 运行状态：番茄钟运行（处于专注模式下）且音乐播放时，专辑封面覆盖一层模糊效果替代原有背景（如蓝背景）。
       - 非运行状态：番茄钟不运行时（处于休息模式或者未启动模式）或者未播放音乐时，恢复原背景，不显示专辑封面背景。
       - 设置选项：在设置中添加开关，允许用户自行对选择模式、休息模式、未启动模式以及暂停模式分别选择使用“专辑封面背景”还是“纯色背景”。

3. 番茄钟页面 UI 细节优化
   (a) 元素位置：
       - 顶部为 Header，中间为倒计时。
       - 左下角任务：目前与边缘有间距，应调整为紧贴左下角。
       - 右下角按键（休息和跳过阶段）：目前有间距，应调整为紧贴右下角。
   (b) 响应式布局：
       - 元素应随浏览器窗口大小动态变动。
       - 窗口过小时，不要出现文字换行，而是通过改变排版等方式进行适配。

4. 后续规划
   (a) 主页模块化：主页计划改造成模块化设计，但目前优先完成上述番茄钟页面的修改。

目前此任务仍未做完。最可用版本为best，关于目前的各个版本：

1. Playlist 版本
   由 Gemini 修改，主要问题在于音乐 API 无法获取歌曲，原因尚不明确。

2. Latest 版本
   由 Copilot 修改，目前核心问题是时间显示的大小不正确，导致播放控制栏等界面元素全部消失。

3. 无后缀版本（沙盒）
   这是最初的主要版本，进行了最多的调整。但由于 Copilot 的错误操作，目前被作为沙盒使用。

4. 副本版本
   这是更早期的版本，应该是添加音乐播放列表之前的一个备份。

https://api-enhanced-eight-gamma.vercel.app


(API KEY)

MUSIC_U=; __csrf=c49db050a0225f205ed59e7899e29222;

